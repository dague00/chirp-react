import React from 'react';

interface props {
    username: string,
    body: string,
    likes: [],
    comments: [],
    timestamp: string
}

export const ChirpView: React.FC<props> = (props) =>{


    return <></>;
}